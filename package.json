{
  "name": "my-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Personal accounting site (frontend + Node/Express backend)",
  "scripts": {
    "start": "node server/index.js",
    "migrate": "node server/migrate.js",
    "postinstall": "npm --prefix server install --omit=dev"
  },
  "engines": {
    "node": ">=20"
  }
}
